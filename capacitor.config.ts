import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Geolocalizacion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
